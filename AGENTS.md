# 项目规范

## 0. 适用范围与优先级

- 适用于本 vault 内所有笔记的新建与修改。
- 冲突时优先级：功能正确性 > 可读性 > 排版偏好。
- 修改既有笔记时，默认对整篇回扫排版规则；若只想改局部，请在指令中说明。

## 1. 图片文件存放

- 生成或插入文档的图片，其原始文件统一存放在项目根目录下的 `_assets/images/` 中（Obsidian 附件目录已指向此处）；目录不存在时先创建。
- 引用写法为 `![[_assets/images/<name>.png|900]]`，默认宽度 900。
- 命名采用 `<主题>-<序号>-<简述>.png`，例如 `05-attention-qkv-lookup.png`；不使用哈希文件名。
- 避免将图片原始文件散落在项目根目录或其他目录；发现散落在别处的图片，移动到 `_assets/images/` 并同步更新引用。

## 2. LaTeX 排版

### 2.1 需要转换

- 数量、计数、索引：`$3$ 个 head`、`第 $2$ 行`、`$N=128$`。
- 变量、数学符号与表达式：`$d_h$`、`$QK^\top/\sqrt{d_h}$`。
- 数字与单位连用：数字放入数学环境，单位保留普通文本，例如 `$1$ fps`、`$2$ bytes`；百分号统一写 `$50\%$`。
- 大数字使用 `{,}` 分隔：`$1{,}050{,}624$`。
- 区间与范围：数字进数学环境，连接符留普通文本，例如 `$1$--$3$`、`$256$--$512$`。

### 2.2 不转换

- Markdown 结构语法：标题编号 `## 4.1`、有序列表序号 `1.`。
- 编号引用与标签：第 3.1 节、第 1(a) 题、Figure 1、`fp16`。
- 普通单词、缩写、代码、命令、文件路径、链接及 frontmatter。
- 表达语感的中文数字：`两个目标`、`三种情况`。

### 2.3 书写细则

- 行内内容使用一对单美元符号 `$...$` 包围，例如 `$1$ fps`、`$N$`、`$x + y$`；独立成行的公式使用双美元符号 `$$...$$` 包围；不使用 `\(...\)`。
- 数学环境内出现中文时使用 `\text{...}`；函数名使用 `\operatorname{}`，例如 `$\operatorname{softmax}$`。
- 同一篇文档内的 shape 记法保持一致：`$[B,N,D]$` 与 `$\mathbb{R}^{B\times N\times D}$` 二选一。

## 3. Frontmatter

- 修改既有笔记时，将 `updated:` 更新为当天日期。
- 新建笔记时，`created:` 与 `updated:` 取同一天日期。

## 4. 提交前自检

- 行内 `$` 成对出现；代码、代码块、链接内没有误加 `$`。
- 图片引用指向的路径真实存在。
- 未改变原有公式的语义。
